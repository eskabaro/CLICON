'use client'

import { FC } from 'react'
import { Container } from '../container'
import styles from './HeaderNav.module.scss'

export const HeaderNav: FC = () => {
    return <Container className={styles.wrapper}></Container>
}
